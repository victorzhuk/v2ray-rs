## 1. Settings and probe

- [x] 1.1 `HealthCheckSettings { enabled: true, failover: false }` as `AppSettings.health_check` with `#[serde(default)]` in `crates/core/src/models/settings.rs`; test: a settings TOML without the table loads with the defaults and round-trips; `timeout 5m cargo test -p v2ray-rs-core -- --test-threads=4` green
- [x] 1.2 `crates/subscription/src/health.rs`: `probe_via_http_proxy(proxy: SocketAddr, url: &str, timeout: Duration) -> Result<(), String>` using `reqwest::Proxy::all` with the existing features (confirm the pinned `reqwest` 0.13 exposes HTTP proxies without extra features before writing it); status < 400 → `Ok`; tests with a local `TcpListener` acting as an HTTP proxy that answers `HTTP/1.1 204` → `Ok`, answers `502` → `Err`, never answers with a 300 ms timeout → `Err` containing `timed out`; `timeout 5m cargo test -p v2ray-rs-subscription -- --test-threads=4` green

## 2. Health state machines

- [x] 2.1 Pure `HealthTracker` in `crates/ui/src/connection.rs` (or a new `health.rs` module in the ui crate): `record(Result<(), String>) -> Option<Health>` returns a transition only, where `record(Ok(()))` reports `Some(Health::Healthy)` whenever `last_reported != Some(Health::Healthy)` — the unhealthy→healthy edge and the first success after `new()`/`reset()`, so the spec's "a successful probe resets that count" has an edge to fire on; tests: 2 failures → none, 3rd → `Unhealthy(reason)`, further failures → none, success → `Healthy`, `first_success_of_a_session_reports_healthy`, `reset()` → healthy with zero count
- [x] 2.2 Pure `DnsFailureWindow`: `observe(line, now)` counts only lines containing `app/dns: failed to retrieve response`, `is_failing(now)` true at ≥ 20 in 60 s, clears 60 s after the last counted line; tests with synthetic `Instant`s including 500 `proxy/tun: connection reset by peer` lines → never failing

## 3. Monitor wiring

- [x] 3.1 Connection task spawns the monitor after reporting `Running` when `settings.health_check.enabled`: 10 s initial delay, 30 s interval, probe address from `effective_settings.local_endpoint(http_port)` (the shared helper `confirm-backend-ready-before-running` adds in `crates/core/src/models/settings.rs`; unspecified → loopback of the same family), url and timeout from `settings.real_delay`; pauses on `Starting`, resets on `Running` via the state receiver; halted with the forwarders on every exit path; emits `AppMsg::ConnectionHealth(generation, Health)` on transitions; stub test with a fake proxy listener that starts failing → one `Unhealthy` message, restored → one `Healthy`, and `handle.stop()` leaves no probe traffic afterwards (listener sees no further connection)
- [x] 3.2 Log forwarder feeds `DnsFailureWindow` when backend is xray and emits `AppMsg::DnsHealth(generation, bool)` on changes; stub test: xray settings, stub prints 25 matching lines → one `true`; sing-box settings with the same output → no message

## 4. App, status bar, notifications

- [x] 4.1 `app.rs` stores health for the current generation, clears it on any non-`Running` state; `update_status_labels` uses the pure `status_texts(&StatusView) -> (String, String)` the sprint mandates, adding `health: Option<Health>` and `dns_failing: bool` to that struct and the three `Running` arms in the sprint's precedence order (unhealthy, then dns_failing, then plain Connected); if `StatusView` is still absent when this lands, introduce it with `state`, `meta`, `health`, `dns_failing` only, leaving `prev_state`/`origin`/`attempt` for `log-connection-decisions`; unit tests for the three spec scenarios and for `Starting` ignoring stale health
- [x] 4.2 On `Unhealthy` transition: toast `Proxy not responding: <reason>` and, when `notifications_enabled`, a desktop notification through a new `TrayHandle` notify method in `crates/tray`; pure `should_announce(prev, next) -> bool` tested so a repeated `Unhealthy` does not announce
- [x] 4.3 Health failover: when `health_check.failover`, no direct `SessionTarget`, and `health_failovers < MAX_AUTO_RECONNECTS`, set an excluded node, increment the counter, and disconnect/reconnect with `ConnectOrigin::AutoReconnect` through a distinct `health_reconnect_pending` flag consumed in the terminal branch of `AppMsg::ProcessStateConnection` before the `reconnect_after_stop(&state, self.reconnect_pending)` check — never through `reconnect_pending`, whose route hardcodes `ConnectOrigin::Restart` and would clear `excluded_node` and `health_failovers` via `cancels_auto_reconnect` before the planner filters candidates; `Connect` drops the excluded node from `planner.plan` for that attempt only; `Healthy` and user Connect/ConnectToNode/Disconnect reset the counter; pure `health_failover_allowed(enabled, session_target, count) -> bool` and `exclude_candidate` unit-tested (direct target → false, count 3 → false)
- [x] 4.4 Preferences Network page: "Check connection health" and "Reconnect when the proxy stops responding" switch rows bound to `health_check.enabled` / `health_check.failover`; manual check that toggling persists to `settings.toml` and does not raise the restart banner

## 5. Verification

- [x] 5.1 `timeout 10m cargo test --workspace -- --test-threads=4` green
- [ ] 5.2 Live, xray: connect, then block the node's address on the host (`sudo nft add rule inet filter output ip daddr <node-ip> drop` in a test table, removed afterwards) → within ~2 minutes the status shows `Proxy not responding` with one toast; removing the rule returns `Connected` without a toast; with failover on and a strategy-planned session, the reconnect picks a different node
